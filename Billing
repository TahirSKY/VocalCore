<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="VocalCore's secure billing system powered by Paddle - Coming Soon">
    <title>VocalCore Billing (Coming Soon)</title>
    <style>
        :root {
            --primary: #00f5d4;
            --bg-dark: #0f172a;
            --text-light: #e2e8f0;
            --accent: #6366f1;
        }
        
        body {
            font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: var(--bg-dark);
            background-image: 
                radial-gradient(circle at 10% 20%, rgba(0, 245, 212, 0.1) 0%, transparent 20%),
                radial-gradient(circle at 90% 80%, rgba(99, 102, 241, 0.1) 0%, transparent 20%);
            color: var(--text-light);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            text-align: center;
            position: relative;
            overflow-x: hidden;
            line-height: 1.5;
        }

        .container {
            padding: clamp(1.5rem, 5vw, 3rem);
            width: min(700px, 100% - 2rem);
            margin-inline: auto;
            background: rgba(255, 255, 255, 0.03);
            border-radius: 24px;
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            box-shadow: 
                0 8px 32px rgba(0, 0, 0, 0.1),
                0 2px 8px rgba(0, 0, 0, 0.05);
            animation: fadeIn 0.8s cubic-bezier(0.16, 1, 0.3, 1);
        }

        @keyframes fadeIn {
            from { 
                opacity: 0; 
                transform: translateY(20px); 
            }
            to { 
                opacity: 1; 
                transform: translateY(0); 
            }
        }

        h1 {
            color: var(--primary);
            font-size: clamp(1.875rem, 5vw, 2.5rem);
            margin-bottom: 1.5rem;
            font-weight: 700;
            letter-spacing: -0.02em;
            position: relative;
        }

        h1::after {
            content: '';
            display: block;
            width: 60px;
            height: 4px;
            background: var(--primary);
            margin: 1rem auto;
            border-radius: 2px;
        }

        p {
            color: var(--text-light);
            line-height: 1.8;
            font-size: clamp(1rem, 2vw, 1.1rem);
            margin: 1.5rem 0;
        }

        .badge {
            background: rgba(99, 102, 241, 0.2);
            color: var(--primary);
            padding: 0.8rem 1.5rem;
            border-radius: 12px;
            display: inline-block;
            margin: 1rem 0;
            font-weight: 500;
            border: 1px solid rgba(99, 102, 241, 0.3);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .badge:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(99, 102, 241, 0.2);
        }

        .cta-button {
            display: inline-flex;
            align-items: center;
            background: var(--accent);
            color: white;
            padding: 1rem 2rem;
            border-radius: 12px;
            text-decoration: none;
            font-weight: 500;
            margin-top: 2rem;
            transition: transform 0.3s ease, box-shadow 0.3s ease, background-color 0.3s ease;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .cta-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 24px rgba(99, 102, 241, 0.3);
            background: #7175f8;
        }

        .cta-button:focus-visible {
            outline: 2px solid var(--primary);
            outline-offset: 2px;
        }

        .tech-circle {
            position: absolute;
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            pointer-events: none;
        }

        .tech-circle-1 {
            width: 300px;
            height: 300px;
            top: -150px;
            right: -150px;
            border-color: rgba(0, 245, 212, 0.1);
        }

        .tech-circle-2 {
            width: 200px;
            height: 200px;
            bottom: -100px;
            left: -100px;
            border-color: rgba(99, 102, 241, 0.1);
        }

        @media (prefers-reduced-motion: reduce) {
            .container {
                animation: none;
            }
            
            .badge,
            .cta-button {
                transition: none;
            }
        }

        @media (max-width: 768px) {
            .container {
                border-radius: 16px;
            }
        }
    </style>
</head>
<body>
    <div class="tech-circle tech-circle-1" aria-hidden="true"></div>
    <div class="tech-circle tech-circle-2" aria-hidden="true"></div>
    <main class="container">
        <h1>Secure Billing & Subscriptions</h1>
        <div class="badge">Powered by Paddle (Coming Soon)</div>
        <p>
            We're engineering a state-of-the-art billing system to ensure 
            enterprise-grade security and seamless payment processing.
        </p>
        <p>
            Our system is currently in final testing phase, with advanced 
            features and robust security protocols being implemented.
            <br>
            <strong>Launch Date: February 2025</strong>
        </p>
        <a href="https://vocalcore.pro/waiting-list/" class="cta-button">
            Join the Waitlist →
        </a>
    </main>
</body>
</html>
